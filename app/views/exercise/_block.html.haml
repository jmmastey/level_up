= cache ['v1', current_user, block.skill] do
  .panel.panel-primary.exercise{ id: block.skill.handle }
    .panel-heading
      %h3.panel-title
        .fa.fa-balance-scale
        = "Exercise: #{block.skill.name} "
    .panel-body
      = render block.questions
      .completion
        - if current_user.signed_in?
          .fa.fa-spinner.fa-spin.spinner
          %button.btn-raised{ 'class' => completion_classes(block.skill), 'data-target' => skill_complete_path(block.skill) }
            %i.fa.fa-square-o
            %i.fa.fa-check-square-o
            Complete
          = render partial: "feedback/link"
