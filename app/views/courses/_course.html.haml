.list-group-item.row.shadow-z-1{ class: ['course', course.handle] }
  .col-lg-5
    .page-header
      %h1= course.name
    %p.description= course.description
    %hr

    %p.completion= completion_summary_for(course)
    .progress
      .progress-bar.progress-bar-success{"data-width" => completion_percent_for(course)}

  .col-lg-7
    %ul.categories.list-group
      = render collection: course.categories.sorted, partial: 'courses/category'
